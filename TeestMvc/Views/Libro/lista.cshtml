@model IEnumerable<TeestMvc.Models.Libro>

@{
    ViewBag.Title = "lista";
}

<h2>lista</h2>

<p>
    @Html.ActionLink("Agregar Libro", "Agregar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.precio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.precio)
        </td>
        <td>
            @Html.ActionLink("Edit", "editar", new {  id=item.codigo }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "eliminar", new {  id=item.codigo  })|
        </td>
    </tr>
}

</table>
<div style="padding:10px; border:1px solid black">
    <div><a href="@Url.Action("printRpt",new {id= "PDF"})"> Get Report PDF</a></div>
    <div><a href="@Url.Action("printRpt",new {id= "Excel"})"> Get Report Excel</a></div>
    <div><a href="@Url.Action("printRpt", new { id = "Word" })"> Get Report Word</a></div>
    <div><a href="@Url.Action("printRpt", new { id = "Image" })"> Get Report Image</a></div>
</div>
